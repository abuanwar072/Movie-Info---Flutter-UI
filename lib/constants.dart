import 'package:flutter/material.dart';

class AppConstants {

  static const secondaryColor = Color(0xFFFE6D8E);
  static const textColor = Color(0xFF12153D);
  static const textLightColor = Color(0xFF9A9BB2);
  static const fillStarColor = Color(0xFFFCC419);

  static const defaultPadding = 20.0;

  static const defaultShadow = BoxShadow(
  offset: Offset(0, 4),
  blurRadius: 4,
  color: Colors.black26,
);
}
